import React from 'react';

const PaginationComponent = () => {
    return (
        <div>
            <button> prev</button>
            <button> next</button>
        </div>
    );
};

export default PaginationComponent;