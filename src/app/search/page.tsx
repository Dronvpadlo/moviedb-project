import React from 'react';
import HeaderComponent from "@/components/HeaderComponent/HeaderComponent";

const Page = () => {
    return (
        <div>
            <HeaderComponent/>
            <input type="text"/>
            <button>Search</button>
            todo realise searching the movies
        </div>
    );
};

export default Page;